<div {{ attributes }}>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex gap-1">
        <button type="submit" class="btn btn-primary">Save</button>
        {% if isEditing %}
        {{ component('delete_modal', {
            trick: trickForm
        }) }}
        {% endif %}
    </div>
    {{ form_end(form) }}
</div>
